extends Node

enum BallType {NONE, CUE, EIGHT, FULL, HALF}
enum PocketLocation {NONE, UP_LEFT, UP, UP_RIGHT, DOWN_LEFT, DOWN, DOWN_RIGHT}
enum GameState {NONE, QUEUE, ROLLING, BALL_IN_HAND}
enum QueueControl {DRAG, MOUSE_WHEEL, TOUCH}
